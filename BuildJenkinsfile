def nexusSnapshotsId = 'nexus-snapshots'
def nexusSnapshotsUrl = 'https://nexus.luxhub.local:9443/repository/maven-snapshots/'
def nexusReleasesId = 'nexus-releases'
def nexusReleasesUrl = 'https://nexus.luxhub.local:9443/repository/maven-releases/'
def groupId = 'com.luxhub.agora'
def artifactId = 'agora-front'
def artifactFile = "${artifactId}-${release.version}-bin.zip"

pipeline {

    agent {
        label 'master'
    }

    stages {
        stage('Dependencies') {
            tools {
                nodejs "nodejs-14.15.0"
            }
            options {
                timeout(time: 20, unit: 'MINUTES')
            }
            steps {
                sh "git checkout ${params.branch}"
                sh 'node -v'
                sh 'npm --version'
                sh 'npm install'
            }
        }

        stage('Build') {
            tools {
                nodejs "nodejs-14.15.0"
            }
            options {
                timeout(time: 10, unit: 'MINUTES')
            }
            steps {
                sh 'npm run build'
                zip dir: 'dist', zipFile: "${artifactFile}", archive: true
            }
        }

        stage('Publish') {
            tools {
                maven 'maven-3.6.0'
            }
            steps {
                script {
                    def packageJson = readJSON file: 'package.json'
                    echo packageJson.name
                    echo packageJson.version
                    echo packageJson.description
                }
                sh "mvn deploy:deploy-file -Dfile=${artifactFile} -DrepositoryId=${nexusSnapshotsId} -Durl=${nexusSnapshotsUrl} -DgroupId=${groupId} -DartifactId=${artifactId} -Dclassifier=bin -Dversion=${release.version}"
            }
        }
    }
}
